<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.spartascout">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VRActivity"
            android:label="Virtual Reality" />
        <activity
            android:name=".VoiceAssistantActivity"
            android:label="Voice Assistant" />
        <activity
            android:name=".PubNubActivity"
            android:label="Publish Location" />
        <activity
            android:name=".CrimeReportActivity"
            android:label="Crime Report" />
        <activity
            android:name=".CampusNewsActivity"
            android:label="Campus News" />
        <activity
            android:name=".TwoDMapActivity"
            android:label="2D Map" />
        <activity
            android:name=".VRInfoActivity"
            android:label="VR Info Screen"
            android:screenOrientation="landscape"
            android:configChanges="keyboardHidden|orientation|screenSize"/>
        <activity
            android:name=".VRInfoActivity2"
            android:label="VR Info Screen 2"
            android:screenOrientation="landscape"
            android:configChanges="keyboardHidden|orientation|screenSize"/>
        <activity
            android:name=".VRInfoPreActivity"
            android:label="VR Info Pre Activity" />
        <activity
            android:name=".VirtualTourActivity"
            android:label="Virtual Tour" />
        <activity
            android:name=".VirtualTourActivity2"
            android:label="Virtual Tour 2" />
        <activity
            android:name=".PathsActivity"
            android:label="Offline Routes" />
        <activity android:name=".HomeActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="626508538025-unp8m3mlstr7doa8eo12f4t1g9l9cgfk.apps.googleusercontent.com"

            />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>